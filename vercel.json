{
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install && cd api && npm install",
  "functions": {
    "api/upload.js": {
      "maxDuration": 60
    },
    "api/process.js": {
      "maxDuration": 120
    },
    "api/validate-pages.js": {
      "maxDuration": 30
    }
  }
}